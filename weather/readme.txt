PROJECT NAME: React JS Weather App

Installation:
1. Navigate to the project directory 'cd project path' 
2. Install dependencies (npm i)

USAGE:
1. After navigating to the project path, give the command in terminal as- npm start
2. Open the browser and navigate to the local host(http://localhost:3000)